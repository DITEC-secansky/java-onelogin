# docker-compose.yml je len tenky "alias" na localhost (demo-local) rezim.
# Vsetok obsah sa importuje z docker-compose.demo-local.yml, takze
#   docker compose up -d
# spusti to iste ako
#   docker compose -f docker-compose.demo-local.yml up -d
include:
  - docker-compose.demo-local.yml

